### 解释：对问题进行规范化解释

这段内容本质上不是“模型回答”，而是一个 **Jinja2 chat template**。它的作用是把结构化输入:

* `messages`
* `tools`
* `add_generation_prompt`
* `enable_thinking`

渲染成一整段模型真正看到的提示词文本。

你可以把它理解成一台“**消息格式化机器**”:

1. 先看有没有 `tools`
2. 再决定 system prompt 怎么写
3. 再把历史消息按角色一条条拼起来
4. 如果 assistant 之前调用过工具，就把工具调用转成 `<tool_call>` XML
5. 如果工具返回了结果，就把结果包成 `<tool_response>` XML
6. 最后如果要继续生成，就补一个 `<|im_start|>assistant`

所以，**读 chat template，不要逐行死看，要先看“它最后想拼出什么字符串”**。

---

### 举例：举例说明问题，帮助理解这个模板到底在做什么

假设输入是这样的：

```json
{
  "tools": [
    {
      "type": "function",
      "function": {
        "name": "get_weather",
        "parameters": {
          "type": "object",
          "properties": {
            "city": { "type": "string" }
          }
        }
      }
    }
  ],
  "messages": [
    { "role": "system", "content": "你是一个天气助手" },
    { "role": "user", "content": "帮我查一下北京天气" }
  ],
  "add_generation_prompt": true
}
```

这个模板最终会大致渲染成：

```txt
<|im_start|>system
你是一个天气助手

# Tools

You may call one or more functions to assist with the user query.

...
<tools>
{"type":"function","function":{"name":"get_weather",...}}
</tools>

For each function call, return a json object with function name and arguments within <tool_call></tool_call> XML tags:
<tool_call>
{"name": <function-name>, "arguments": <args-json-object>}
</tool_call><|im_end|>
<|im_start|>user
帮我查一下北京天气<|im_end|>
<|im_start|>assistant
```

模型看到这段后，就知道：

* 自己是 assistant
* 有哪些工具可用
* 调工具时必须输出 `<tool_call>...</tool_call>`
* 现在该继续生成 assistant 内容了

这就是 chat template 的核心作用。

---

### 知识点：着重说明知识点

#### 1. 整体结构怎么读

这段模板建议按 4 层来读：

1. **开头的 tools/system 分支**
2. **中间的状态变量计算**
3. **主体消息循环**
4. **末尾的 generation prompt**

如果你一上来就盯着 `{{- ... -}}` 和 `{%- ... -%}`，会很容易迷路。

#### 2. Jinja 语法先抓几个最关键的

* `{{ ... }}`：输出内容
* `{% ... %}`：流程控制，比如 `if / for / set`
* `{{-` 或 `{%-`：左侧去空白
* `-}}` 或 `-%}`：右侧去空白

所以这类模板一半是“逻辑”，一半是“最终输出的字符串”。

例如：

```jinja
{{- "<|im_start|>assistant\n" -}}
```

意思不是执行 assistant，而是**把这段文本原样输出**。

#### 3. 第一大块：有 tools 时，先重写 system prompt

开头这一段：

```jinja
{%- if tools -%}
  {{- "<|im_start|>system\n" -}}
  ...
  {{- "# Tools\n\nYou may call one or more functions ..." -}}
  ...
{%- elif messages[0].role == "system" -%}
  {{- "<|im_start|>system" + messages[0].content + ... -}}
{%- endif -%}
```

它的含义是：

* 如果有 `tools`
  * 就主动构造一个很长的 system prompt
  * 把原始 system message 内容塞进去
  * 再补上工具说明
  * 再告诉模型工具调用格式必须是 `<tool_call>...</tool_call>`
* 如果没有 tools，但第一条消息本来就是 `system`
  * 那就把原始 system message 正常输出

也就是说，这个模板把“工具说明”直接并入了 system prompt。

#### 4. 为什么只在 `messages[0].role == "system"` 时处理一次

这是为了避免：

* system prompt 在前面已经输出过一次
* 后面遍历消息时又重复输出第一条 system

所以你会看到后面还有一段判断：

```jinja
{%- if message.role == "user" or message.role == "system" and not loop.first -%}
```

意思是：

* `user` 都正常输出
* `system` 只有不是第一条的时候才在这里输出

第一条 system 已经在最上面处理掉了。

#### 5. 第二大块：`ns.multi_step_tool` 和 `ns.last_query_index` 在干什么

这一段是全模板里最绕的地方：

```jinja
{%- set ns = namespace(multi_step_tool=true, last_query_index=messages | length - 1) -%}
{%- for message in messages[::-1] -%}
  ...
  {%- if ns.multi_step_tool and message.role == "user" and ... -%}
    {%- set ns.multi_step_tool = false -%}
    {%- set ns.last_query_index = index -%}
  {%- endif -%}
{%- endfor -%}
```

它是在**倒序扫描消息**，找“最后一个真正的用户问题”在哪里。

为什么要找这个位置？

因为后面 assistant 消息里可能带有：

* `reasoning_content`
* `<think>...</think>`
* 工具调用链

模板作者不希望把所有历史 assistant 的思维内容都完整暴露出来，所以做了一个边界：

* `last_query_index` 之前的 assistant 消息，正常渲染
* `last_query_index` 之后的 assistant 消息，如果有 reasoning，可能带上 `<think>...</think>`

简单理解：

* 它在识别“最近一轮用户问题之后”的对话片段
* 这通常对应当前这一轮工具调用与推理链

#### 6. 第三大块：消息循环是模板主体

核心循环是：

```jinja
{%- for message in messages -%}
```

然后按角色分别处理。

##### `user` / `system`

```jinja
{{- "<|im_start|>" + message.role + "\n" + content + "<|im_end|>\n" -}}
```

这部分最直白，就是包装成：

```txt
<|im_start|>user
...
<|im_end|>
```

或者：

```txt
<|im_start|>system
...
<|im_end|>
```

##### `assistant`

assistant 是最复杂的，因为它除了普通内容，还要处理：

* `reasoning_content`
* 历史内容里手写的 `<think>...</think>`
* `tool_calls`

先看这段：

```jinja
{%- set reasoning_content = "" -%}
{%- if message.reasoning_content is string -%}
  {%- set reasoning_content = message.reasoning_content -%}
{%- elif "</think>" in content -%}
  {%- set reasoning_content = content.split("</think>")[0] ... -%}
  {%- set content = content.split("</think>")[-1] ... -%}
{%- endif -%}
```

意思是：

* 如果结构化字段里已经有 `reasoning_content`，直接用它
* 否则，如果 `content` 里已经手写了 `<think>...</think>`，就把它拆出来

也就是说，这个模板兼容两种来源的“思维内容”：

1. 独立字段 `reasoning_content`
2. 直接嵌在文本中的 `<think>...</think>`

#### 7. assistant 的 `<think>` 什么时候会被输出

关键判断在这里：

```jinja
{%- if loop.index0 > ns.last_query_index -%}
  {%- if loop.last or not loop.last and reasoning_content -%}
    {{- "<|im_start|>assistant\n<think>\n" + reasoning_content ... + content -}}
  {%- else -%}
    {{- "<|im_start|>assistant\n" + content -}}
  {%- endif -%}
{%- else -%}
  {{- "<|im_start|>assistant\n" + content -}}
{%- endif -%}
```

可以粗略理解为：

* 在“最近一次用户请求之后”的 assistant 消息里
  * 如果是最后一条，或者带 reasoning
  * 就把 `<think>` 一起输出
* 更早的 assistant 历史消息
  * 只按普通 assistant 内容输出

这说明该模板是支持“显式思维块”的。

#### 8. tool call 是怎么拼出来的

如果 assistant message 里带了 `tool_calls`，就继续输出：

```jinja
<tool_call>
{"name": "...", "arguments": ...}
</tool_call>
```

这一段做了两层兼容：

```jinja
{%- if tool_call.function -%}
  {%- set tool_call = tool_call.function -%}
{%- endif -%}
```

说明输入里的工具调用对象可能长这样：

```json
{
  "function": {
    "name": "get_weather",
    "arguments": "{\"city\":\"Beijing\"}"
  }
}
```

也可能已经是扁平结构：

```json
{
  "name": "get_weather",
  "arguments": { "city": "Beijing" }
}
```

模板两种都兼容。

另外这里还专门判断了：

* `arguments` 是字符串时，直接输出
* `arguments` 不是字符串时，走 `tojson`

所以最终一定会变成合法 JSON。

#### 9. tool 响应为什么被包成 user

这一段也很关键：

```jinja
{%- elif message.role == "tool" -%}
  {%- if loop.first or messages[loop.index0 - 1].role != "tool" -%}
    {{- "<|im_start|>user" -}}
  {%- endif -%}
  {{- "\n<tool_response>\n" -}}
  {{- content -}}
  {{- "\n</tool_response>" -}}
  {%- if loop.last or messages[loop.index0 + 1].role != "tool" -%}
    {{- "<|im_end|>\n" -}}
  {%- endif -%}
{%- endif -%}
```

含义是：

* 多条连续 `tool` 消息，不会每条都新开一个 role block
* 它们会被合并在一个 `<|im_start|>user ... <|im_end|>` 里面
* 每条 tool 结果再用 `<tool_response>...</tool_response>` 包起来

这是一种常见技巧：

* 模型先以 assistant 身份发出 `<tool_call>`
* 外部执行工具
* 再把工具结果“喂回去”
* 喂回去时，不一定真的使用 `tool` role，而是转成一段用户侧输入文本

等于告诉模型：

* 这是工具执行结果
* 你现在基于它继续回答

#### 10. 最后一块：为什么结尾会补一个 assistant 起始标记

收尾这段：

```jinja
{%- if add_generation_prompt -%}
  {{- "<|im_start|>assistant\n" -}}
  {%- if enable_thinking is defined and enable_thinking is false -%}
    {{- "<think>\n\n</think>\n\n" -}}
  {%- endif -%}
{%- endif -%}
```

它表示：

* 如果当前要让模型继续生成
* 就补一个新的 assistant 开头

也就是告诉模型：**下面该你说了**。

而：

```jinja
enable_thinking is defined and enable_thinking is false
```

这一段表示：

* 如果明确关闭 thinking
* 仍然输出一个空的 `<think></think>`

这通常是为了让输出格式保持稳定，或者兼容下游解析器。

---

### 阅读顺序：以后你再看类似模板，可以按这个顺序拆

1. 先找最外层有没有 `if tools`
   看它是不是把工具说明并进 system prompt。
2. 再找有没有 `namespace(...)`
   这通常说明模板里有“状态变量”或“多轮逻辑”。
3. 再找主循环 `for message in messages`
   这是模板主体。
4. 再按 role 拆：
   `system`、`user`、`assistant`、`tool` 各自怎么格式化。
5. 最后看 `add_generation_prompt`
   它决定最终是不是要给模型留一个“继续生成”的入口。

---

### 简化版伪代码：把这份模板压缩成你真正该记住的逻辑

```python
if tools:
    输出 system 起始
    如果第一条消息是 system:
        输出原始 system 内容
    输出工具说明和工具 schema
    输出工具调用格式说明
elif 第一条消息是 system:
    输出原始 system

倒序扫描 messages
找到最后一个真正的 user query 的位置 last_query_index

for message in messages:
    if role 是 user / 非首条 system:
        正常输出 <|im_start|>role ... <|im_end|>

    elif role 是 assistant:
        提取 reasoning_content
        根据 last_query_index 决定是否输出 <think>
        如果有 tool_calls:
            输出 <tool_call>{"name": ..., "arguments": ...}</tool_call>

    elif role 是 tool:
        把连续 tool 响应合并包装为 user 块
        每条工具结果包成 <tool_response>...</tool_response>

if add_generation_prompt:
    输出 <|im_start|>assistant
    如果关闭 thinking:
        输出空的 <think></think>
```

---

### 一句话总结：这段模板最重要的 5 个点

1. 它是 **Jinja2 模板**，不是模型输出内容本身。
2. 它的任务是把 `messages + tools` 转成模型实际看到的 prompt。
3. 它把工具说明塞进了 system prompt。
4. 它支持 `<think>`、`<tool_call>`、`<tool_response>` 三种特殊块。
5. 它最后用 `add_generation_prompt` 决定是否补一个新的 assistant 开头。

---

### 你看这类模板时最容易误判的地方

* 误以为 `{{ "<|im_start|>assistant" }}` 是模型在回答
  实际上这是模板在“拼接字符串”。
* 误以为 `tool` role 一定会原样保留
  这里没有，它被包进 `user` block 里了。
* 误以为 `<think>` 总是来自模型原文
  这里既可能来自 `reasoning_content` 字段，也可能来自已有内容拆分。
* 误以为每条历史 assistant 都会带 reasoning
  这里通过 `last_query_index` 做了条件控制。

---

### 结论：你应该怎么理解这份模板

这份模板属于一种比较完整的“**支持工具调用 + 支持显式思维块 + 支持多轮工具链**”的 chat template。

如果你以后要分析类似模板，核心不是去背每个 Jinja 细节，而是先问 4 个问题：

1. system prompt 是怎么组织的？
2. tools 是怎么注入的？
3. assistant 的 tool call 和 think 是怎么编码的？
4. tool result 是怎么回灌给模型的？

你把这 4 个问题搞清楚，基本就能把一份 chat template 读明白。
