# 2.Chat模板

> 说明：本文根据 Hugging Face LLM Course 第 11 章第 2 节整理，为中文翻译版。  
> 原文源码：`chapters/en/chapter11/2.mdx`

# Chat Templates

## 简介

Chat 模板对于组织语言模型与用户之间的交互非常重要。无论你是在做一个简单聊天机器人，还是构建更复杂的 AI Agent，正确地组织对话格式，都是拿到更好模型效果的关键。

本节会介绍：

- Chat 模板是什么
- 为什么它重要
- 如何正确使用它

> 提示
>
> Chat 模板之所以重要，是因为它能帮助你：
>
> - 维持一致的对话结构
> - 正确标识不同角色
> - 在多轮对话中管理上下文
> - 支持工具调用等高级特性

## 模型类型与模板

### Base Model 和 Instruct Model

Base Model 是在原始文本数据上训练出来的，目标通常是预测下一个 token；而 Instruct Model 则是在此基础上，进一步微调成更擅长遵循指令和进行对话的模型。

例如：

- [`SmolLM2-135M`](https://huggingface.co/HuggingFaceTB/SmolLM2-135M) 是一个 base model
- [`SmolLM2-135M-Instruct`](https://huggingface.co/HuggingFaceTB/SmolLM2-135M-Instruct) 是它的指令微调版本

经过 instruction tuning 的模型学会了遵循特定的对话结构，因此更适合聊天应用。而且，instruct 模型通常还能处理更复杂的交互，例如：

- 工具调用
- 多模态输入
- function calling

如果想让一个 base model 以更像 instruct model 的方式工作，就需要用模型能理解的一致格式来组织 prompt。这正是 Chat 模板的作用。

ChatML 就是一种常见模板格式。它用清晰的角色标记（如 `system`、`user`、`assistant`）来组织对话。

参考：

- ChatML 示例：<https://huggingface.co/HuggingFaceTB/SmolLM2-135M-Instruct/blob/e2c3f7557efbdec707ae3a336371d169783f1da1/tokenizer_config.json#L146>

> 警告
>
> 使用 instruct model 时，一定要确认你采用了正确的 chat template。模板用错了，会直接导致模型表现变差，甚至出现异常行为。最简单的确认方式，就是去 Hub 上查看该模型的 tokenizer 配置。

## 常见模板格式

在看具体实现前，先理解不同模型对对话格式的要求有何不同。

以下是统一使用的示例对话：

```python
messages = [
    {"role": "system", "content": "You are a helpful assistant."},
    {"role": "user", "content": "Hello!"},
    {"role": "assistant", "content": "Hi! How can I help you today?"},
    {"role": "user", "content": "What's the weather?"},
]
```

这是 SmolLM2、Qwen 2 一类模型常用的 ChatML 格式：

```sh
<|im_start|>system
You are a helpful assistant.<|im_end|>
<|im_start|>user
Hello!<|im_end|>
<|im_start|>assistant
Hi! How can I help you today?<|im_end|>
<|im_start|>user
What's the weather?<|im_start|>assistant
```

这是 `mistral` 常见的模板格式：

```sh
<s>[INST] You are a helpful assistant. [/INST]
Hi! How can I help you today?</s>
[INST] Hello! [/INST]
```

它们的核心差异包括：

1. **系统消息的处理方式不同**
   - Llama 2 用 `<<SYS>>` 标签包裹系统消息
   - Llama 3 使用 `<|system|>` 标签，并用 `</s>` 结束
   - Mistral 把系统消息合并进第一条 instruction
   - Qwen 用 `<|im_start|>` 之类的角色标记
   - ChatGPT 风格常见 `SYSTEM:` 前缀

2. **消息边界的标记方式不同**
   - Llama 2 常用 `[INST]` 和 `[/INST]`
   - Llama 3 使用角色专属标签
   - Mistral 结合 `[INST]` 与 `<s>` / `</s>`
   - Qwen 使用角色起止 token

3. **特殊 token 的使用不同**
   - 有些模型用 `<s>`、`</s>` 表示对话边界
   - 有些模型在每条消息后都补一个结束 token

理解这些差异，是正确使用不同模型的前提。

## transformers 如何自动处理模板

好消息是，`transformers` 库通常会根据模型自动应用正确的模板：

```python
from transformers import AutoTokenizer

mistral_tokenizer = AutoTokenizer.from_pretrained("mistralai/Mistral-7B-Instruct-v0.1")
qwen_tokenizer = AutoTokenizer.from_pretrained("Qwen/Qwen-7B-Chat")
smol_tokenizer = AutoTokenizer.from_pretrained("HuggingFaceTB/SmolLM2-135M-Instruct")

messages = [
    {"role": "system", "content": "You are a helpful assistant."},
    {"role": "user", "content": "Hello!"},
]

mistral_chat = mistral_tokenizer.apply_chat_template(messages, tokenize=False)
qwen_chat = qwen_tokenizer.apply_chat_template(messages, tokenize=False)
smol_chat = smol_tokenizer.apply_chat_template(messages, tokenize=False)
```

也就是说，同样的 `messages`，不同 tokenizer 会按各自模型要求输出不同格式。

## 高级功能

Chat 模板不仅可以处理普通对话，还可以支持更复杂的场景，例如：

1. **工具调用**
2. **多模态输入**
3. **函数调用**
4. **多轮上下文维护**

> 提示
>
> 在实现这些高级能力时：
>
> - 一定要针对你的具体模型做充分测试
> - 仔细观察不同特性带来的 token 消耗
> - 把各类输入格式写成文档，避免后续使用混乱

### 多模态输入示例

```python
messages = [
    {
        "role": "system",
        "content": "You are a helpful vision assistant that can analyze images.",
    },
    {
        "role": "user",
        "content": [
            {"type": "text", "text": "What's in this image?"},
            {"type": "image", "image_url": "https://example.com/image.jpg"},
        ],
    },
]
```

### 工具调用示例

```python
messages = [
    {
        "role": "system",
        "content": "You are an AI assistant that can use tools. Available tools: calculator, weather_api",
    },
    {"role": "user", "content": "What's 123 * 456 and is it raining in Paris?"},
    {
        "role": "assistant",
        "content": "Let me help you with that.",
        "tool_calls": [
            {
                "tool": "calculator",
                "parameters": {"operation": "multiply", "x": 123, "y": 456},
            },
            {"tool": "weather_api", "parameters": {"city": "Paris", "country": "France"}},
        ],
    },
    {"role": "tool", "tool_name": "calculator", "content": "56088"},
    {
        "role": "tool",
        "tool_name": "weather_api",
        "content": "{'condition': 'rain', 'temperature': 15}",
    },
]
```

## 最佳实践

使用 Chat 模板时，建议遵循这些原则：

1. **格式保持一致**
   - 整个应用里尽量只使用同一种模板格式。
2. **角色定义清楚**
   - 每条消息都明确写清楚它属于 `system`、`user`、`assistant` 还是 `tool`。
3. **做好上下文管理**
   - 注意多轮对话积累后带来的 token 长度问题。
4. **做好异常处理**
   - 尤其是工具调用、多模态输入这些复杂场景。
5. **发送前先校验消息结构**
   - 不要把格式错误的消息直接送进模型。

> 警告
>
> 常见错误包括：
>
> - 在同一个应用里混用多种模板格式
> - 对话历史太长导致超出 token 限制
> - 没有正确处理特殊字符
> - 输入消息结构不合法
> - 忽略模型自身对模板格式的特殊要求

## 动手练习

可以尝试把 `HuggingFaceTB/smoltalk` 数据集转换成 ChatML 风格：

1. 加载数据集

```python
from datasets import load_dataset

dataset = load_dataset("HuggingFaceTB/smoltalk")
```

2. 编写转换函数

```python
def convert_to_chatml(example):
    return {
        "messages": [
            {"role": "user", "content": example["input"]},
            {"role": "assistant", "content": example["output"]},
        ]
    }
```

3. 再使用目标模型的 tokenizer 应用对应的 chat template

最终要确认生成出来的格式，确实符合目标模型的要求。

## 额外资源

- Hugging Face Chat Templating 指南：<https://huggingface.co/docs/transformers/main/en/chat_templating>
- Transformers 文档：<https://huggingface.co/docs/transformers>
- Chat Templates 示例仓库：<https://github.com/chujiezheng/chat_templates>
